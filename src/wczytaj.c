#include <stdio.h>
#include "graf.h"

/*
struct graf *wczytaj_graf(FILE *in, struct graf *g)
{
    while (!feof(in))
    {
        // fscanf(in, "%d %d", &)
        for (int x = 0; x < g->cells; x++)
        {
            for (int y = 0; y < g->tab[x]->cells; y++)
                fscanf(in, "\t %d :%lf", &g->tab[x]->tab[y]->_do, &g->tab[x]->tab[y]->waga);
        }
    }
}
*/
